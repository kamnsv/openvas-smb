clear
rm -d -r build
rm -d -r inst
mkdir build
cd build
cmake -DCMAKE_INSTALL_PREFIX=/home/oazpo/openvas-smb/inst ..
make                # build the libraries
make install        # install the build
cd ..
cd inst/bin
ls
